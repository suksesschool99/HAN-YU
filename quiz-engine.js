// =========================================================================
// DINOSAUR QUIZ ENGINE (ALIASED TO YCT 2 ENGINE)
// =========================================================================

// Load and export DinoQuizEngine
if (typeof DinoQuizEngine === 'undefined' && typeof window !== 'undefined' && window.DinoQuizEngine) {
  // DinoQuizEngine already bound to window
}
